install the necessary dependencies listed in `requirements.txt`, you can do that by executing the following command: `pip install -r requirements.txt`. Additionally, it is required to download and install FFmpeg, ensuring that it is added to your system's environment path for proper functionality. Also download ollama and type `ollama run llama3:8b` or whatever model you want to use it.